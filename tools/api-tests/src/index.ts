/*
 * # This module is mainly a library so it only exports stuff from its api-module
 *
 * Eventually, this could act as a source of truth for the API and be used as a commandline utility to generate graphql definitions
 *
 */

console.log("This library is for testing only, use `yarn test` to run the tests");

export * from "./xpub-api";
