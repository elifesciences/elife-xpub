const f = fixture('DeleteSubmission')

f.before(() => Promise.resolve())
  .beforeEach(() => Promise.resolve())
  .afterEach(() => Promise.resolve())

test('Delete a Submission', async t => {
  await Promise.resolve()
})
