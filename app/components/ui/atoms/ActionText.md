A portion of text that has an action associated with clicking it.

```js
      <ActionText onClick={() => alert('do action')}>
        Pre-fill my details
      </ActionText>{' '}
      using ORCID
```
