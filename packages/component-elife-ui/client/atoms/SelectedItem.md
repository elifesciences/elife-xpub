Represents a selected item in a multiselect form control.

    <SelectedItem label="Ecology" onCloseClick={() => console.log('Close')} />

Several on a line

    <div>
      <SelectedItem label="One"/> <SelectedItem label="Two"/>
    </div>
